/**
 *@description 로그인 페이지
 */
function LoginPage() {
  return (
    <div
      style={{
        maxWidth: 1200,
        margin: "40px auto",
        display: "flex",
        flexDirection: "column",
        justifyContent: "space-between",
        gap: 32,
      }}
    ></div>
  );
}

export default LoginPage;
